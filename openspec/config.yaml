# SDD Configuration — omarchy-nix
# Auto-generated by sdd-init on 2026-06-27
---
project: omarchy-nix
description: >
  NixOS flake porting DHH's Omarchy Hyprland desktop (Arch Linux) to NixOS.
  Provides nixosModules.default and homeManagerModules.default for an opinionated
  Hyprland+NixOS desktop experience with 22 themes, webapp focus-or-launch,
  gaming/NVIDIA/FIDO2/voxtype support, and 40+ utility scripts.

stack:
  - NixOS flakes (nixpkgs nixos-unstable)
  - Home Manager (nix-community/home-manager master)
  - Hyprland v0.54.3 (hyprwm/Hyprland)
  - nix-colors (misterio77/nix-colors)
  - walker (abenz1267/walker v2.15.2)

detected_at: "2026-06-27T00:00:00Z"

strict_tdd: false
strict_tdd_reason: >
  No test runner, test framework, or CI found. This is a Nix config project with
  no unit/integration/E2E tests. nix flake check is the only validation tool.
  Strict TDD requires a testable harness which does not exist here.

testing:
  test_runner: null
  test_layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  quality_tools:
    linter: false
    type_checker: false
    formatter:
      name: alejandra
      command: "alejandra ."
  notes: >
    Validation is done via nix flake check (syntax + eval) and nixos-rebuild
    dry-build (full config evaluation). No formal test framework is available.

persistence:
  mode: hybrid
  openspec_dir: openspec/
  engram_topic_key: sdd-init/omarchy-nix

phases:
  propose:
    allowed: true
    template: changes/{change-name}/proposal.md
  spec:
    allowed: true
    template: changes/{change-name}/spec.md
  design:
    allowed: true
    template: changes/{change-name}/design.md
  tasks:
    allowed: true
    template: changes/{change-name}/tasks.md
  apply:
    allowed: true
    template: changes/{change-name}/apply.md
  verify:
    allowed: true
    template: changes/{change-name}/verify.md
  archive:
    allowed: true
    template: changes/{change-name}/archive.md
